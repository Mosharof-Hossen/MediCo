import axios from 'axios';

const instance = axios.create({
    baseURL: "",
    withCredentials: true
})
const useAxios = () => {

    return (
        <div>

        </div>
    );
};

export default useAxios;